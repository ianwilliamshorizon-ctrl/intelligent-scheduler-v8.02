rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /vehicle-media/{imageId} {
      allow read, write: if true; // In production, we should restrict to authenticated users
    }
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
